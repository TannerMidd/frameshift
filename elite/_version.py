"""Application version. The release workflow overwrites this file at build time
from the git tag (see .github/workflows/release.yml); the value committed here
is the fallback for source runs."""

VERSION = "2.1.4"
